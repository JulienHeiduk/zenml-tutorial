[project]
name = "zenml-tutorial"
version = "0.1.0"
description = "Hands-on ZenML tutorial for the team"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "zenml[server]>=0.68.0",
    "scikit-learn>=1.3",
    "pandas>=2.0",
    "pyarrow>=14.0",
]

[tool.uv]
package = false
